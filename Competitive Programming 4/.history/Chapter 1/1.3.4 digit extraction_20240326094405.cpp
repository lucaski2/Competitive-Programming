#include <bit>

using namespace std;


int main(void)
{
    int N; scanf("%d\n", &N);

    while (N--)
    {
        char x[110];
        scanf("0.%[0-9]...\n", &x)
        printf("the digits are 0.%s")
    }



    // for (int i = 0; i < )
}
